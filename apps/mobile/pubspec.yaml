name: pytone_mobile
description: Pytone Digital mobile app — Android / iOS / iPadOS.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0
  media_kit: ^1.1.11
  media_kit_video: ^1.2.5
  cached_network_image: ^3.4.0
  grpc: ^4.0.1
  protobuf: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
