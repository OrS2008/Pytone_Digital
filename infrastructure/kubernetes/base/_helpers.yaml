# Shared labels for all Pytone resources. We use the recommended
# Kubernetes labels — observability tools and dashboards key off these.
#
# Apply to every workload object:
#   labels:
#     app.kubernetes.io/name: gateway
#     app.kubernetes.io/component: backend
#     app.kubernetes.io/part-of: pytone
#     app.kubernetes.io/managed-by: helm
